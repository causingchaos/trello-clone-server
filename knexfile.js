// config file for knex is stored in /config, just importing 
// for manual use of knex migrations and seeds
// knexjs requires a knexfile.js in the root of server
const config = require('./config/knexfile.js');
module.exports = config;